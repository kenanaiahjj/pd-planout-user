
  # PlanOut Prototype

  This is a code bundle for PlanOut Prototype. The original project is available at https://www.figma.com/design/vuY07DVRTFR4T0py6yYCMh/PlanOut-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  